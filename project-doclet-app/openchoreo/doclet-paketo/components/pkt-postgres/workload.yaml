apiVersion: openchoreo.dev/v1alpha1
kind: Workload
metadata:
  labels:
    openchoreo.dev/component: "pkt-postgres"
  name: "pkt-postgres-workload"
  namespace: default
spec:
  container:
    env:
      - key: POSTGRES_USER
        value: doclet
      - key: POSTGRES_PASSWORD
        value: doclet
      - key: POSTGRES_DB
        value: doclet
    image: "postgres:16-alpine"
  endpoints:
    tcp:
      port: 5432
      type: TCP
  owner:
    componentName: "pkt-postgres"
    projectName: "doclet-paketo"
